#!/bin/bash

# Used for testing out the language server using Neovim
#
# local configs = require('lspconfig.configs')
#
# if not configs.just_lsp then
#   configs.just_lsp = {
#     default_config = {
#       cmd = { '/.../just-lsp/bin/start' },
#       filetypes = { 'just' },
#       root_dir = function(name)
#         return lsp.util.find_git_ancestor(name)
#       end,
#       settings = {},
#     },
#     on_attach = on_attach
#   }
# end
#
# lsp.just_lsp.setup({})

cd ~/src/just-lsp && cargo run

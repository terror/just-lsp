---
title: Installation
order: 10
---

Install `just-lsp` with [Cargo](https://doc.rust-lang.org/cargo/), the Rust
package manager:

```bash
cargo install just-lsp
```

Make sure the installed `just-lsp` binary is on your `PATH` so your editor can
find it, then follow the [usage instructions](#usage).
